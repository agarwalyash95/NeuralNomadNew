# Current Project State

Last reconciled: **2026-07-20 (Asia/Calcutta) — Reference Foundation Phase 09 complete (self-verified); Phase 10 BLOCKED (not executed)**
Baseline inspected: branch `main`, commit `a386842`

This is a dated checkpoint, not a replacement for `git status`, current code, or runtime evidence.

## Current focus

**Reference Foundation execution checkpoint (2026-07-20): Phases 00–09 are complete; Phase 10 is BLOCKED, not executed.** Phase 00 closed **PASS WITH CONDITIONS**; Phase 01 closed **PASS**; Phase 02 closed **PASS**; Phase 03 closed **PASS WITH CONDITIONS**; Phase 04 closed **PASS WITH CONDITIONS**; Phase 05 closed **PASS WITH CONDITIONS**; Phase 06 closed **PASS WITH CONDITIONS**; Phase 07 closed **PASS WITH CONDITIONS**; Phase 08 closed **PASS WITH CONDITIONS**; **Phase 09 now closes PASS WITH CONDITIONS, self-verified (see `docs/plans/phases/phase-09-verification-report.md`). Phase 10 was investigated fresh and found blocked on all 4 of its own gates — documented, not forced (see `docs/plans/phases/phase-10-blocked-status.md`).** This closes the master plan's full 00-10 phase sequence: 9 phases genuinely complete, 1 honestly documented as not-yet-possible.

Phase 09 summary: built `reference_dashboard --json`, aggregating (via `call_command`, never re-implementing) every existing standalone report/benchmark command from Phases 3-8 plus a new cross-job `PlanGenerationJob.usage` aggregation and real `EXPLAIN` output for the master plan's 4 named query shapes — live-verified against the real dev DB, every section returning real data. **A real boundary violation was introduced and fixed within the same pass**: the dashboard's first draft imported `apps.planner.models` directly (D-004), caught by this phase's own `check_layer_boundaries` step and fixed by moving the aggregation into a new planner-owned `generation_usage_summary` command. Built a real N+1 audit (`scripts/phase9_n_plus_1_audit.py`, no such harness existed in the repo before) against the real S11 workspace's already-generated trip data — found real, proportionate query-scaling in `validate_plan` (this session's own Phase 8 geo-sanity check, architecturally expected) and pre-existing `_stamp_transit_hints` code, neither remotely close to the `station_selector` severity fixed the same day. Cache-key registry + recovery runbook docs consolidate real existing patterns (6 real Redis namespaces; the `pg_dump`/`pg_restore` procedure already used identically 4 times). Materialized route summaries' own trigger condition was real when this phase started but was resolved by a same-day separate fix before this phase's work finished — decision recorded as no-longer-needed.

Phase 10 status: **BLOCKED, zero deletions performed.** Fresh evidence (not assumed from prior docs) confirms none of Phase 10's 4 gates are met: knowledge-app removal (§12 step 8, a real production beat-cycle confirmation, never ran); attractions-app removal (§13's own named blocker — no `apps.reference` paginated browse endpoint — still open, owner approval never sought); legacy-resolver removal (`PLANNER_ROUTE_GRAPH_ENABLED` still `False`, still owner-gated — though the specific latency defect that made the flip unconsiderable was fixed the same day, see Phase 8's completion note); `check_layer_boundaries` empty-allowlist criterion (still 2 entries, the anticipated lower-layer geocoding writer never built). **A real false claim in Phase 10's own scope text was also corrected**: "delete the 3 haversine wrappers" — verified all three are live, real call paths today, not dead code. Full gate-by-gate detail and exact closing conditions: `docs/plans/phases/phase-10-blocked-status.md`.

Phase 08 summary: fresh investigation found two of this phase's own master-plan claims were false/premature before any code was written — "_price_transport_blocks on price_estimator, done in P5" was false (that function calls only `lookup_live_price`; no kill-switch of any kind exists anywhere to remove), and "journey flow fully on route_graph.search" was premature (Phase 4's own reports explicitly gate that flip on an owner decision). Built and ran, for real, Phase 4's own explicitly-deferred prerequisite — a `route_graph.search()` timing benchmark. **Real, serious finding: median latency ~8 seconds across 13 real city-pair searches, worst case 48 seconds** — confirms Phase 4's own prior suspicion about `station_selector`'s unbounded query loop, with real numbers instead of a hunch. Flagged as background task `task_ab02ca90`, not fixed this phase (separate, focused debugging work; `PLANNER_ROUTE_GRAPH_ENABLED` stays `False`, zero production impact). Built the two named validation additions for real — `_validate_day_price_sanity` (cab/bus/train only, reusing real `transit_hints` distance data, never guessed) and `_validate_day_geo_sanity` (block-vs-day-city-centroid) — both live-verified against real seeded data with a genuine positive and negative case each, surfaced through the *existing* scorecard `reasons` mechanism with zero schema change. S11 regression re-run for real, identical to Phase 4's recorded evidence. `npx tsc --noEmit` run for real (clean) to verify the "frontend untouched" claim directly rather than asserting it. Full detail: `docs/plans/phases/phase-08-implementation-report.md`.

Phase 07 summary: a structural migration, not a data import — `apps.knowledge`'s 5 live models (`EntityEmbedding`/`DistanceEdge`/`PlaceInsight`/`LocalTip` → `apps.reference`; `PlanInsightDismissal` → `apps.planner`) relocated via Django's `SeparateDatabaseAndState` recipe (real tables untouched, `db_table` pinned explicitly since none had an override before), and 8 confirmed-dead models (zero real usages by fresh grep *and* zero rows in every one at time of deletion) dropped for real. Real starting-state investigation found heavier coupling than the master plan's own table implied — 5 `KnowledgeEngine.resolve()` call sites in `reference/views.py` alone, 3 Celery tasks that were pure delegating wrappers, and 3 independent planner entry points into the embeddings module where the plan's text only named one — ~25 real call sites were migrated in total. The `entity_embedding_hnsw` pgvector index and all 5 relocated tables' row counts were verified byte-identical against the live DB at three separate checkpoints across the migration, and `pg_tables` was queried directly to confirm the 8 dead tables are genuinely gone. Parity proven three ways: identity checks (the relocated names are the literal same Python objects between old and new import paths — the strongest possible evidence, not behavioral similarity), a live behavioral comparison of the one genuine wrapper (`KnowledgeEngine.resolve()` vs the new `resolve_places()`), and a real `DistanceEdge` write/read round-trip. `check_layer_boundaries --strict-knowledge` (Phase 7's own named acceptance bar) passes with zero violations — **a real boundary violation was introduced and fixed within the same pass** (the first shim draft re-exported a planner-owned model from the knowledge app, tripping D-004; removed once confirmed no real caller needed it, rather than adding a new allowlist exception). **A real, unrelated Phase 6 bug was found during this phase's own parity testing and flagged, not fixed**: `import_osm_places` never set `external_id` on created rows, so all 8,124 OSM-imported places fail the publishability check and are invisible to the cache-hit path — every `explore()` call for an OSM-only city still makes a live paid Google Places call. Scoped to steps 1–7 of the master plan's 10-step sequence — step 8 (confirming parity after a real production Celery beat cycle) can't be verified synchronously in one session, so steps 8–10 (beat-cycle confirmation, shim removal, app removal) are an explicit owner-timed follow-up; `apps.knowledge` remains in `INSTALLED_APPS`. Full detail: `docs/plans/phases/phase-07-implementation-report.md`.

Phase 07 summary: a structural migration, not a data import — `apps.knowledge`'s 5 live models (`EntityEmbedding`/`DistanceEdge`/`PlaceInsight`/`LocalTip` → `apps.reference`; `PlanInsightDismissal` → `apps.planner`) relocated via Django's `SeparateDatabaseAndState` recipe (real tables untouched, `db_table` pinned explicitly since none had an override before), and 8 confirmed-dead models (zero real usages by fresh grep *and* zero rows in every one at time of deletion) dropped for real. Real starting-state investigation found heavier coupling than the master plan's own table implied — 5 `KnowledgeEngine.resolve()` call sites in `reference/views.py` alone, 3 Celery tasks that were pure delegating wrappers, and 3 independent planner entry points into the embeddings module where the plan's text only named one — ~25 real call sites were migrated in total. The `entity_embedding_hnsw` pgvector index and all 5 relocated tables' row counts were verified byte-identical against the live DB at three separate checkpoints across the migration, and `pg_tables` was queried directly to confirm the 8 dead tables are genuinely gone. Parity proven three ways: identity checks (the relocated names are the literal same Python objects between old and new import paths — the strongest possible evidence, not behavioral similarity), a live behavioral comparison of the one genuine wrapper (`KnowledgeEngine.resolve()` vs the new `resolve_places()`), and a real `DistanceEdge` write/read round-trip. `check_layer_boundaries --strict-knowledge` (Phase 7's own named acceptance bar) passes with zero violations — **a real boundary violation was introduced and fixed within the same pass** (the first shim draft re-exported a planner-owned model from the knowledge app, tripping D-004; removed once confirmed no real caller needed it, rather than adding a new allowlist exception). **A real, unrelated Phase 6 bug was found during this phase's own parity testing and flagged, not fixed**: `import_osm_places` never set `external_id` on created rows, so all 8,124 OSM-imported places fail the publishability check and are invisible to the cache-hit path — every `explore()` call for an OSM-only city still makes a live paid Google Places call. Scoped to steps 1–7 of the master plan's 10-step sequence — step 8 (confirming parity after a real production Celery beat cycle) can't be verified synchronously in one session, so steps 8–10 (beat-cycle confirmation, shim removal, app removal) are an explicit owner-timed follow-up; `apps.knowledge` remains in `INSTALLED_APPS`. Full detail: `docs/plans/phases/phase-07-implementation-report.md`.

Phase 06 summary: real `pg_dump` taken first (required per the master plan's own global rule; `docs/plans/evidence/phase-06/backup-confirmation.md`). `PlaceCrossIdMixin` (`wikidata_id`/`osm_id`/image-rights fields) added to all 4 place-entity master tables — confirmed zero cross-id fields existed anywhere on them before this phase — via migration `reference.0017_phase6_place_enrichment`, additive. **Two real, flagged deviations from the master plan's literal wording, both because real investigation showed the literal path wasn't cleanly buildable**: (1) `import_osm_places` uses the free **Overpass API** instead of a Geofabrik PBF extract, since `osmium`/`pyosmium` is confirmed not installed and a full India extract is a multi-GB download — same underlying ODbL OSM data, bounded per-city queries, no new binary dependency; a real undocumented `User-Agent` header requirement was found (via a `406` failure) and fixed. (2) `import_asi_monuments` was **not built** — real web research found no licence-verified name+coordinates+price dataset for ASI monuments anywhere (data.gov.in's ASI-tagged resources are small parliamentary-question aggregates; ASI's own site is scanned-PDF-only) — deferred honestly, same discipline as Phase 4's `BusRoute`/Phase 5's train fares. `import_osm_places` **live-verified against a real 3-city pilot** (Bengaluru/Delhi/Mumbai): **8,124 real rows created**, 22 backfilled, 2,474 correctly skipped as ambiguous rather than blindly merged; a spot-checked row confirmed the honesty design goal directly (real `osm_id`/coordinates, `price_range`/`user_rating` both `None` — never fabricated). Mumbai's first attempt hit a genuine Overpass `504`, made zero partial writes, and succeeded cleanly on retry. `CategoryVocabularyMap` was built **smaller than the plan's own premise** — investigation found `places_explore.py` never normalized Google's raw place types at all, so only the real 11 anchors that exist in code were seeded, not a speculative full taxonomy. A new `audit_reference_data` report (10) surfaces Google-sourced rows past their TTL — live-verified with a synthetic aged/fresh/never-enriched test trio, then run for real (0 stale currently). The attractions-app §13 audit resolved the plan's own flagged unknown (zero planner/booking references to the old app, independently re-verified by grep) and named the real remaining retirement blocker precisely (no `apps.reference` paginated browse-list endpoint exists yet to replace a live frontend dependency) — no frontend/code change attempted, correctly out of this backend-only phase's scope. Restaurant price-unit normalization stays deferred, same reasoning as Phase 5. Full detail: `docs/plans/phases/phase-06-implementation-report.md`.

Phase 05 summary: a `FareRule` model (migration `reference.0016_phase5_price_estimation`, additive) plus `reference/services/price_estimator.py` — one honest six-class price ladder replacing the hardcoded price literals scattered across `transport_compare.py` (cab), `suggestions.py` (restaurant), and `recommendations.py`/`conversation_engine.py` (destination-tier budget + daily food). `TravelPriceObservation` — a model with **zero writers anywhere in the codebase** before this phase despite already being fully modeled/admin-registered/serialized — now has two real writers (a single funnel-point hook in `ProviderRegistry.search()`, and a paired write alongside `live_price.py`'s existing `TravelPriceHistory` creation), live-verified. `rollup_price_summaries` and `evaluate_price_estimators` (§10.5 offline holdout backtest) both built and live-verified with real statistical computation, not stubs. Seeded fare data: cab (the exact already-shipped ₹300+₹16/km, zero fabrication risk) and bus (a real, dated UPSRTC published rate, national-fallback caveat, confidence discounted) — **train/metro were deliberately left unseeded**, since IRCTC's real distance-slab fares are only published as scanned/binary PDFs this session's tooling couldn't extract text from; nothing was transcribed from memory and presented as sourced fact. Found and fixed two real, separate pre-existing bugs while wiring this up: a wrong-FK bug in the cab benchmark lookup (caught by testing against real rolled-up data), and a live `NameError` in `conversation_engine.py::_compute_recommended_budget_inr` (referenced two names never imported into that file) — fixed by deleting the broken duplicate and delegating to the now-canonical `recommendations.recommended_budget_inr`. **Found and flagged (not fixed) a third, unrelated pre-existing bug** during end-to-end verification: `transport_compare.py::_resolve_city`'s naive substring city match can resolve "Agra" to "Lagrange, US", silently producing an absurd fare from a corrupted distance — a background-task chip was raised for the owner rather than fixed opportunistically. `_price_transport_blocks` (plan_generation.py) was deliberately not touched (out of this phase's approved scope) — still prices cab/bus/train solely via the pre-existing `lookup_live_price` path. Full detail: `docs/plans/phases/phase-05-implementation-report.md`, `phase-05-verification-report.md`, and `docs/plans/evidence/phase-05/` (backfilled 2026-07-20, same day, later session — the original reports described live-verification that was never saved as evidence files; every check was re-run fresh against the real dev DB for the backfill). The flagged `transport_compare.py::_resolve_city` bug was fixed in a separate session (now tries `name__iexact` before falling back to `name__icontains`); the evidence backfill found the identical unguarded-icontains pattern still present in this phase's own `live_price.py::_resolve_observation_fk` (cab branch) — flagged as background task `task_8c7fcda3`, not fixed.

Phase 03 summary: source-registry/reconciliation machinery live; GODL-India licence verified in full; first real imports (City +80, 4,169 geonameid, 9,582 aliases, 667 districts, cross-IDs on most airports). Full detail: `docs/plans/phases/phase-03-implementation-report.md`.

Phase 04 summary: **`AirportRoute`/`TrainRoute`/`BusRoute`/`HubTransferLink`/`MetroArea` were all at zero rows going in** — Phase 4 was "populate from scratch," not "enrich," and is documented as such. Onboarded a new licence-verified source (OpenFlights, ODbL, honestly marked stale-since-2014) alongside the already-approved datameet source, populating 3,485 real `TrainRoute` rows and 1,916 `AirportRoute` + 79 `Airline` rows from zero. Added route-model extensions + `HubTransferLink` (migration `reference.0015_phase4_route_graph`, additive). Built `reference/services/route_graph.py` (the V1 multimodal search algorithm) and refactored `journey_resolver.py` into a thin adapter — legacy path preserved verbatim, new `PLANNER_ROUTE_GRAPH_ENABLED` flag (default **False**, ships inert everywhere) selects the authoritative implementation, `PLANNER_MULTIMODAL_SHADOW_MODE` now does real work. **A real regression was found and fixed via the shadow-mode check itself**: the route_graph path initially lost the train option entirely for the real Kolkata→Gangtok/Pelling workspace (S11) because it required a literal scheduled-edge row with no geometric fallback; fixed, re-verified clean twice more. **H7 (`backfill_station_intelligence` wipe-and-rebuild) fixed via incremental upsert and live-verified**: the command was deliberately killed mid-run via SIGTERM twice during this session, and both times immediate row-count checks confirmed zero data loss. Two real, separate performance bugs were found this way and fixed (missing bounding-box pre-filter causing ~107M unfiltered haversine calls; a floating-point exact-equality comparison spuriously flagging nearly every row "changed" every run) — the command now completes in 25m33s. S1–S14 scenario suite: 13/14 via real pytest tests (all passing), S11 via a dedicated real-workspace shadow-comparison script. Reports 5/6 live; **report 7's full 190-pair run did not finish in session** — root-caused to `station_selector`'s own unbounded per-hub-pair query loop (pre-existing, out of this phase's scope to fix), not a new defect. Durable records: `docs/plans/phases/phase-04-implementation-report.md`, `phase-04-verification-report.md`, and `docs/plans/evidence/phase-04/`.

**Known, flagged, not-yet-fixed items from Phase 03** (all low-risk, non-blocking):
- 4 pre-existing duplicate `State` rows (e.g. "Uttar-Pradesh" with 14 cities vs "Uttar Pradesh" with 0 cities). Safe to merge/delete later, not attempted (no-deletion scope).
- 6 City duplicate-candidate pairs surfaced by report 3 — worth a human preview pass through `merge_reference_entities`; none auto-merged.
- 12 City + several RailwayStation cross-id resolutions differed between two independent passes over identical source data — flagged as `DataQualityIssue`, never overwritten, root cause untracked.

**Known, flagged, not-yet-fixed items from Phase 04** (all low-risk, non-blocking):
- Report 7's full top-200-pair run did not complete in session; `station_selector`'s unbounded candidate-hub-pair query loop is the identified cause — a pre-existing characteristic, not a Phase 4 defect. A future session should either cap it or accept the longer runtime.
- `BusRoute` stays at 0 rows — no free, reliable India intercity bus-route dataset was found.
- `HubTransferLink` only reached 5 rows out of 50 candidate top-population cities — most don't have a matching primary railway+airport (or +bus) hub pair under the current `is_primary_hub` criteria.
- The large railway/airport ServiceArea churn from the Phase 4 backfill re-run (614k→771k railway rows net; real coordinate differences between stored and freshly-refetched datameet coordinates) was not traced to specific stations.
- `route_graph.search()` was not isolated-benchmarked (no dedicated `EXPLAIN`/timing pass) — recommended before Phase 8 integration.
- `PLANNER_ROUTE_GRAPH_ENABLED` stays **False** everywhere — flipping it is an explicit owner decision after reviewing the shadow-comparison evidence, not something this session did.

Next action: the master plan's phase sequence (00-10) is now fully worked through — 9 phases complete, Phase 10 honestly blocked. There is no "next phase" until the owner closes one of Phase 10's 4 gates (see `docs/plans/phases/phase-10-blocked-status.md` for exactly what each requires) or requests something else. The owner may want an independent verification pass on Phases 03-09 first, since their verification has been self-authored rather than done by a separate reviewer agent (unlike Phases 00-02). Open follow-ups, none blocking: an owner decision on flipping `PLANNER_ROUTE_GRAPH_ENABLED` (the latency blocker is now fixed, but the flip itself remains owner-gated); the OSM-row `external_id` gap from Phase 6/7 (`task_a0b7a620`) — a background session has been working this; `_stamp_transit_hints`'s query-count scaling found during Phase 9's N+1 audit (minor, pre-existing, not urgent); OCR the IRCTC fare-slab PDF for real train fare data (see Phase 05 report); scale `import_osm_places` beyond its 3-city pilot; retry the ASI monuments dataset search; a frontend/product decision on the attractions-app §13 blocker (build a paginated `apps.reference` browse endpoint, or retire the old browse page) — this is also Phase 10's gate 2.

The active initiative is the reference-data system audit, repair, and complete enrichment.
- **Reference Data Audit & Repair: Complete.** Built schema migrations, updated reference models, created canonical resolver and station selector services, registered all new structures in admin/serializers. Successfully populated coordinates for **8,697 railway stations** and generated **43,232 correct station service area mappings** via the optimized `backfill_station_intelligence` command. Missing coordinates reduced from 9,010 to 314.
- **Planner Integration: Complete.** Wired Canonical Resolver and Station Selector into geocoding and journey resolving paths.
- **Verification: Complete.** Django system checks are clean and all programmatic scenario tests pass under pytest (Bombay/Mumbai, Bangalore/Bengaluru, Gurgaon/Gurugram, Delhi NCR, Noida separate, and route-eligibility checks verified).

**Post-completion correction (2026-07-18, same day as full R3–R13 completion):** the owner hit a real regression from R3 live — see `docs/agent/HANDOFF.md` "fifth update" for full detail. **R3 is reverted** (STATE-01/confirmation-card-reachability is an open, unfixed finding again — do not re-attempt it by skipping the normal cluster-asking flow). **R5 is extended**: `journey_resolver.resolve_journey_options` now also backfills city coordinates (previously only `geocoding.resolve_or_create_city` did, which runs too late to help this specific failure path). Both fixes were verified against the owner's real, actually-failing workspace/data, not just synthetic stubs.

**The planner repair sequence in `docs/planner-complete-current-audit-and-repair-plan.md` is now fully complete — all phases A through E.**

- **Phase A — Done.** `_resolve_cities` scoping fix, plus two follow-on Decimal/`JSONField` serialization fixes discovered during the owner's first live attempts.
- **Phase B — Done.** Owner ran a real generation (workspace `be504346-2f80-489d-965f-c93c4112d3bb`: Kolkata→Gangtok/Pelling, 8 days, 2 adults, ₹50,000 budget); agent gathered the full evidence via read-only DB inspection: `degraded=false`, all cities present, real venues, correct nearby-hub transport resolution. Full detail: audit doc §20.
- **Phase C — Done.** Issue register (§16), gap matrix (§17), and architecture verdict (§18) reconciled against that evidence. Architecture verdict **ratified, no longer provisional**.
- **Phases D & E — Done.** All eleven backlog items (R3–R13, audit doc §19) implemented and statically verified, one at a time, after the owner explicitly directed continuing through the full backlog ("go on and do not stop until all phase completed") rather than pausing after each item. Summary (full diffs/evidence in §19 and `docs/agent/HANDOFF.md`):
  - **R3** STATE-01 — Create-card no longer gated behind optional clusters once ready.
  - **R4** GEN-02 — verified already correct for the dev-compose reference (no code change).
  - **R5** TRANS-01/DATA-01 — city-coordinate backfill on re-resolution; `_nearest_hubs` widened beyond country match; origin now prefers its FK like destination already did; exact-match-first city resolution in transport blocks.
  - **R6** CHAT-01 — browse-only turns now get the same "don't ask the next question" reply treatment answer-only turns already had.
  - **R7** CTX-01 — numeric budget + international block now reach prompt text; infants now count in party/room math.
  - **R8** VAL-01 — three new warning-only validation checks (hours conflict, tight travel time, hotel-nights-vs-stay); found and fixed a genuine pre-existing `_compose_days` bug where a repeat-city (loop) visit's night counts collapsed into one.
  - **R9** REC-01 — cross-trip recency now recorded to `TravelerProfile`, not just same-trip; diversity/rotation weights raised.
  - **R10** CANVAS-01 — pending local edits are flushed before a background refetch can rebuild `planData`; city/day merge in `ItineraryTimeline` is now id-based, not positional.
  - **R11** STATE-02/DEAD-01 — confirmed-dead code removed (unreachable block, duplicate `save()`/field/settings lines, two unregistered widgets); the larger origin/budget-read-path unification was deliberately left out of scope as too risky to verify statically.
  - **R12** REL/SEC — REST CAS conflicts now return 409; job reuse against a changed draft is now observable (not force-superseded, by deliberate design); per-session rate caps added on chat/generation; **found and fixed a real auth bug** — the actual login modal only ever wrote tokens to the `neuralnomad-auth` key, but the reload check only read the legacy `accessToken` key.
  - **R13** PROV-01 — transport route provenance now gets its own trust-tier badge (distinct from price provenance); `degraded` now persists durably on the trip itself (previously only on the ~1.8s-visible ephemeral job) and surfaces via a new, distinct banner in the Plan Canvas.
- **Final full-repo verification:** `manage.py check` clean, migration discovery clean, `tsc --noEmit` clean, full `npm run lint` — 38 problems, byte-for-byte identical to the session's starting baseline (all pre-existing, none introduced).

The owner's remaining task on the bug-repair sequence itself is live manual verification of the full flow (see audit doc Final Question 17 for the concrete checklist) — no phase-gate is currently blocking further work there.

**A second, parallel initiative is now active alongside the reference-data work above: the planner north-star roadmap** (`docs/planner-north-star-audit-and-vision.md`, owner-approved 2026-07-18, owner then said "execute all phases now"). This is forward-looking product/UX work — chat completeness, conversational plan editing, rest/return-to-hotel semantics, canvas decision-info, and a longer intelligence/lifecycle backlog — distinct from and does not touch the reference-data redesign or any of its files.

- **Phase 0 (foundations & honesty unblockers) — done** (2026-07-19): the dead `edit_plan` chat stub is revived and honest; a rest/hotel_return non-bookable block taxonomy exists end-to-end (canvas-addable, not yet generator-emitted); `RecommendationEngine`'s per-dimension confidence data is now rendered in the Explain popover using the app's existing trust-tier language; the trip-watch/price-watch Celery loops now write real `Notification` rows; a new read-only endpoint bridges planner-committed bookings into `/vault/bookings`.
- **Phase 1 (total conversational control) — done** (2026-07-19): 6 new chat-edit intents (add rest, add hotel-return, remove any named block, move a block to another day, remove any day with renumbering, swap/add a place via real search) alongside the 3 pre-existing ones, all via the existing `PlanProposal` accept/reject mechanism. Along the way, found and fixed a real gap `accept_proposal` had (no commitment-hierarchy check at all, unlike `patch_trip`/`select_item` — a booked block could have been silently dropped by any proposal) and the frontend gap that fix exposed (a non-409 accept failure showed no message). Full detail: `docs/agent/HANDOFF.md` "planner north-star Phase 1".
- **Phase 2 (living plan on the page) — done** (2026-07-19): a new `TightTravelTimeWarning` insight rule surfaces per-day pacing that was computed but discarded before; all 3 dead compare trays (Hotel/Attraction-Activity/Restaurant) fully wired end-to-end across 11 files (pinning was completely unreachable before — detail panels discarded it via underscore-aliased unused props); forex's Manali-only advisory is now region-gated like visa already was; explore list cards show up to 2 real facts instead of 1 (also fixed a bug where a real priced entry fee was silently dropped); a new `price-lookup` endpoint + manual "Check nightly rate" action for hotels (data-sparse today, ~4 real rows, improves as reference data grows); a deterministic (not LLM-driven) hotel-return anchor now generates automatically at plan-creation time, reusing the exact Phase 1 block shape. Full detail: `docs/agent/HANDOFF.md` "planner north-star Phase 2".
- **Phase 3 (expert reasoning shown, M5) — done** (2026-07-19): inline "considered A, chose B because…" trade-off sentences on generated blocks, computed deterministically from the same `PreferenceScorer` reasons already stamped per-candidate (no new AI call, no fabrication risk) and rendered directly on the plan-canvas card, not gated behind the reactive Explain button; `knowledge/services/embeddings.py::semantic_search` (pgvector RAG, previously referenced only by its own definition and one frontend-unconsumed endpoint) is now a real second candidate source in `_build_candidate_pool`, merged/deduped against the reference-DB rows and still fully subject to the existing ranking/rejection/constraint filters; a new LLM critic pass (`_run_critic_review`) extends the existing deterministic refinement loop — runs only when a plan is still flagged for review after that loop's one re-compose attempt, within the existing AI-call budget, and surfaces concrete itinerary-grounded findings via the existing `CheckoutCanvas` quality banner. Deliberately not built: automatic re-optimization against the critic's findings (a separate, higher-risk "constraint-solver" feature — see `docs/agent/HANDOFF.md` Phase 3 note). Full detail: `docs/agent/HANDOFF.md` "planner north-star Phase 3".
- **Phase 4 (compounding personal memory, M2) — done** (2026-07-19): a real per-user taste vector (stored as an ordinary `TravelerProfile` fact, no new model/migration) blended via slow EMA from real place embeddings on every kept/booked place and swap-away, retrieved by pgvector cosine similarity as a third candidate source in `_build_candidate_pool` alongside the reference-DB rows and M5's text-based semantic retrieval; a new `category_removed` `EditSignal` (trip-wide, ≥2-removal threshold) extends the edit-diff vocabulary from 3 to 4 kinds and feeds a running ± `category_affinity` fact that nudges ranking and is spoken directly to the compose LLM; deterministic episodic trip memory (real kept/removed place titles per destination, never an LLM narrative) retrievable into a later trip to the same place; verified (not rebuilt) that the profile-vocabulary bridge (`promote_draft_preferences_to_profile`) was already wired in ahead of `ConstraintEngine`, with one honest gap left undocumented no longer — `avoid_red_eye` has no real UI signal to bridge, left alone rather than fabricated; `PlannerIntentFlow` (confirmed write-only dead, not even user-scoped, built on a fabricated `completion_rate` metric) removed — migration generated but **not yet applied**, owner must run `python manage.py migrate`; `PlannerQuestionBank` deliberately left as-is (already correctly gated with an on-the-record rationale). A real, separate bug was found and fixed while verifying: `plan_mutations.select_item` aliased `current`/`copied_current` to the same dict, so reading the "old" block's metadata after the in-place swap silently returned the new block's data instead. Full detail: `docs/agent/HANDOFF.md` "planner north-star Phase 4".
- **Phase 5 (chat intake completeness, M4 depth) — done** (2026-07-19): dietary/accessibility now proactively ride the already-mandatory `trip_style` card on full trips (a `_TRIP_STYLE_FIELDS_BY_INTENT` override, essentials left unchanged so the card still auto-skips on budget alone — verified, not assumed); children's ages captured alongside the existing child-count stepper via a new `_PARTY_FIELDS_BY_INTENT` override; an optional per-category budget split (soft compose-prompt guidance, not enforced — no per-category enforcement mechanism exists anywhere downstream); a free-text escape hatch for interests/purpose folded into the existing normalized fields at the `plan_context.py` anti-leak boundary. A real, separate dead-field bug was found and fixed along the way: `FIELD_OPTIONS.accessibility` didn't exist, so the accessibility multi-select would have silently rendered zero buttons no matter how proactively it was surfaced. All new fields are metadata-only (no migration). Full detail: `docs/agent/HANDOFF.md` "planner north-star Phase 5".
- **Flagged for the owner/Codex, not fixed (out of `apps/planner` scope):** `apps/reference/services/live_price.py::lookup_live_price` stamps every live-provider price hit `provenance_tier="verified"` without checking whether `LIVE_PROVIDERS_ENABLED` is actually on — a possible honesty gap if mock inventory is being served.
- **`hotel_return`/`rest` block contract violation — fixed (2026-07-19), post-Phase-5, real production regression.** Owner reported a live `BlockContractViolation` failing real generation on every trip where Phase 2g's `_append_hotel_return_anchors` fires (essentially any multi-day trip with a hotel). Root cause: `block_contract.py` predates the Phase 0b `rest`/`hotel_return` non-bookable taxonomy and required `currency_code`/`master_ref` on every block with no exemption; Phase 2g was the first generation-time path to actually run those blocks through it. Phase 2's own verification tested a *different* validator (`validate_plan`, not `block_contract.validate_days`), which is why this slipped through. Fixed by exempting the two non-bookable categories in `block_contract.validate_block`; verified against the real block-builder functions plus a regression check that bookable-category validation is unchanged. Full detail: `docs/agent/HANDOFF.md` "hotel_return BlockContractViolation" entry.
- **Duplicate-city `place_id` IntegrityError — fixed (2026-07-19), post-Phase-5.** Owner reported a live `reference_city_place_id_key` crash during real generation. Root cause: two `City` rows for the same real place under different name strings (no `CityAlias` linking them) both geocode to the identical `place_id`; `apps/planner/services/geocoding.py::backfill_city_coordinates`/`resolve_or_create_city` assigned it without checking for an existing owner. Fixed entirely within `apps/planner` (ownership check before assigning/creating; converges onto the existing row instead of colliding). Verified with a reproduction script. **A second instance of the identical unguarded-write pattern remains, untouched, in the Codex-owned `apps/reference/management/commands/backfill_city_coordinates.py`** — flagged, not fixed. Full detail: `docs/agent/HANDOFF.md` "duplicate-city IntegrityError fix".
- **Session stopped here by explicit owner direction (2026-07-19).** Phases 0–5 of the north-star roadmap are complete and verified. **Phase 6** (living plan in the world — live flight/train status, a day-of companion mode, offline passes, and a **business-gated** flip of live providers + real payment collection) and **Phase 7** (the whole journey — constraints-first discovery, post-trip REMEMBER, **group/collaborative planning**, i18n/multi-currency, onboarding) are **deliberately deferred**: the owner has no live pricing system and no payment gateway in place yet, and both phases depend on that infrastructure existing first. **Do not start Phase 6 or 7 in a future session without the owner confirming that infrastructure is ready and greenlighting the business decisions involved (gateway choice, PCI scope, in-app collection vs. provider redirect).** When resumed, treat Phase 6 as "propose a scoped plan first," not "build silently" — see `docs/agent/HANDOFF.md`'s Phase 5 entry for the full stop note.

## Verified repository shape

- Frontend: Next.js 15, React 19, TypeScript under `frontend/`.
- Backend: Django 5 / Django REST Framework under `backend/`.
- Data/services: PostgreSQL, Redis, Celery, and Docker Compose.
- Planner frontend entry routes: `frontend/src/app/planner/`.
- Planner backend entry area: `backend/apps/planner/`.
- Canonical pre-generation state: server-side planner draft state.
- Canonical generated plan: server-side `PlannerTrip` with workspace revision/concurrency controls.

## Architecture checkpoint

The audit's architecture verdict is **ratified, not provisional** (Phase C, confirmed by real Phase B generation evidence): preserve and repair the existing boundaries — a full R3–R13 repair pass never found cause to restructure them.

- AI owns intent understanding and planning strategy.
- Deterministic services own facts, entity IDs, candidate selection constraints, provenance, validation, persistence, and concurrency.
- Frontend state is a mirror of server authority and must reconcile by stable IDs/revisions.
- Reference data and provider results must retain honest provenance; estimates must not appear verified.

## Known constraints and risks

- The working tree had **172 status entries** at this reconciliation point. That count will become stale; always inspect the live status.
- The prior planner/backend test suite, testing settings, and `pytest.ini` are deleted in the working tree. This is an observed constraint, not evidence that tests pass.
- Many older plans and architecture documents are deleted or stale. Use them as history only.
- Live provider behavior, Celery worker availability, anonymous-session continuity, and rapid canvas-edit persistence require runtime/manual verification.
- Production disables planner thread fallback and mock booking inventory; worker availability is operationally significant.
- The Decimal→JSONField encoder sweep covers every field in `apps/planner/models.py` only, by deliberate scope decision (see Current focus). If a future change routes a `Decimal` into an `apps/reference` `JSONField`, or into a new `apps/planner` `JSONField` added without `encoder=DjangoJSONEncoder`, the same crash class can reappear.
- `docker-compose.yml`'s `frontend` service now sets `WATCHPACK_POLLING`/`CHOKIDAR_USEPOLLING` because Docker Desktop on Windows frequently fails to propagate native filesystem events for bind-mounted directories into the Linux container, so webpack's default watcher silently stops seeing host-side edits (the dev server keeps running; hot reload just never fires). A source edit that is verified correct by `tsc`/lint but doesn't appear in the browser should prompt checking this before assuming the code is wrong — and remember an env var change requires recreating the `frontend` container to take effect, not just a file save.

## Current verification baseline

- Static inspection confirms `_resolve_cities` now writes each resolved city inside the loop and returns the mapping.
- Static inspection confirms `intelligence/progressive.input_hash` now serializes with `default=str`, matching the sibling hash call in `plan_generation.start_generation_job`.
- Static inspection confirms all 25 `JSONField`s across 11 models in `backend/apps/planner/models.py` carry `encoder=DjangoJSONEncoder` (verified by a shell check comparing every targeted field's `.encoder` attribute).
- `python manage.py check` — clean. `python manage.py makemigrations --check --dry-run` — no changes detected (migrations `0018_jsonfield_decimal_encoder` and `0019_jsonfield_decimal_encoder_remaining` fully capture the model state).
- **A real end-to-end generation run has now been confirmed** — see Current focus above and `docs/planner-complete-current-audit-and-repair-plan.md` §20 for the full evidence. This is no longer an open item.
- **All of R3–R13 are implemented and statically verified** — see audit doc §19 for each item's exact verification (stubbed checks, real-Phase-B-data checks, or read-only confirmation of already-correct code, as appropriate per item). Final full-repo pass: `manage.py check` clean, migration discovery clean, `tsc --noEmit` clean, `npm run lint` unchanged from the session baseline (38 problems, all pre-existing).
- **Not verified live:** none of R3–R13 was exercised through the actual running app/browser by the agent (per standing instruction not to start/stop dev servers, and no large test suite exists to run instead). The owner's live-verification checklist is in the audit doc, Final Question 17.
- The detailed audit reports Django checks, migration discovery, import/startup validation, and frontend type-check for Phase A; rerun relevant checks before relying on that report in a changed tree.
- No full automated regression baseline is currently available from the deleted planner tests. Per owner direction, restoring that suite is explicitly out of scope for this repair sequence.
- **Not yet verified:** STATE-01's fix has not been confirmed in the live browser UI by the owner — only statically/via stubbed check.

## Next checkpoint definition

Update this file when the owner changes priority or current evidence disproves
a material claim above.
